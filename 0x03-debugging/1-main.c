#include <stdio.h

/**
 * main - causes an infinite loop
 * Retrn 0
 */

int main(void)
{
	int i;

	print("Infinite loop incomint :(\n");

	i = 0;
	/**
	 * while (i < 10);
	 * {
	 * 	putchar(i);
	 * }
	 */
	printf("Infinite loop avoided: \\o/\n");

	return (0);
}
